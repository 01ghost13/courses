<table>
  <thead>
    <tr>
      <th><%= sort_link @ransack_search, :name, 'Name' %></th>
    </tr>
  </thead>
  <tbody>
    <% @courses.each do |course| %>
      <tr>
        <td>
          <%= course.name %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= paginate @courses %>
